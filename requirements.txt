# RAG Chatbot - Pinned Dependencies
# ============================================================
# Install order matters: torch MUST come from the CPU-only index
# to avoid pulling 2.6 GB of CUDA wheels on a CPU-only box.
#
# Install command:
#   pip install -r requirements.txt
#   pip install torch==2.12.1 --index-url https://download.pytorch.org/whl/cpu
#
# This file lists ALL dependencies (Phases 1-9) pinned for full
# reproducibility. Components not yet implemented are harmless to
# install - they simply sit unused until their phase activates.

# --- Web framework (Phase 1) ---
fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.13.4
pydantic-settings==2.14.2
python-multipart==0.0.32
starlette==1.3.1

# --- PDF processing (Phase 2) ---
PyMuPDF==1.27.2.3
pytesseract==0.3.13          # Tesseract wrapper; needs system tesseract-ocr binary
rapidocr-onnxruntime==1.2.3  # OCR fallback (pure pip, no binary needed)
Pillow==12.2.0              # Image handling for OCR rendering
onnxruntime==1.27.0         # RapidOCR backend
opencv-python==4.13.0.92    # RapidOCR image preprocessing
pyclipper==1.4.0            # RapidOCR polygon clipping
shapely==2.1.2              # RapidOCR geometry

# --- Text processing (Phase 3) ---
langdetect==1.0.9
transformers==5.12.1         # BGE tokenizer for token-aware chunking
tokenizers==0.22.2           # transformers backend
huggingface_hub==1.20.1      # model/tokenizer download
regex==2026.5.9             # transformers dependency
safetensors==0.8.0          # transformers dependency

# --- Embeddings + vector store (Phase 4) ---
# torch: install separately with --index-url https://download.pytorch.org/whl/cpu
torch==2.12.1
sentence-transformers==5.6.0
faiss-cpu==1.14.3
numpy==2.4.6

# --- LLM answer generation (Phase 5) ---
openai==2.43.0
httpx==0.28.1

# --- Development / testing ---
pytest==9.1.1
httpx2==2.4.0          # Required by starlette.testclient (TestClient)
